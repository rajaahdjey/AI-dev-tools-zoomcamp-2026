[project]
name = "mini-kanban-backend"
version = "0.1.0"
description = "Mini Kanban board backend (FastAPI + SQLite)"
requires-python = ">=3.13"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
]
